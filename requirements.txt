Flask>=3.1
icalendar>=7.1
recurring-ical-events>=3.8
requests>=2.32

# Email "to handle" section (optional — only needed if you connect Gmail)
google-api-python-client>=2.100
google-auth>=2.20
google-auth-oauthlib>=1.0
anthropic>=0.40
